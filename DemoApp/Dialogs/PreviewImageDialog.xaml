<CommonControls:DialogControl x:Class="DemoApp.Dialogs.PreviewImageDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:DemoApp.Dialogs"
        xmlns:Behaviors="clr-namespace:TensorStack.WPF.Behaviors;assembly=TensorStack.WPF"
        xmlns:CommonControls="clr-namespace:TensorStack.WPF.Controls;assembly=TensorStack.WPF"
        xmlns:CommonConverters="clr-namespace:TensorStack.WPF.Converters;assembly=TensorStack.WPF"
        xmlns:Controls="clr-namespace:DemoApp.Controls"
        mc:Ignorable="d"
        MinWidth="400"
        MaxWidth="500"
        Title="Image Preview"
        Icon="{StaticResource ImageIcon}"
        Style="{StaticResource {x:Type CommonControls:DialogControl}}">
    <DockPanel DataContext="{Binding RelativeSource={RelativeSource AncestorType={x:Type local:PreviewImageDialog}}}"  Margin="15, 15, 15, 10">
        <Grid DockPanel.Dock="Bottom">
            <Button Content="Close" Command="{Binding OkCommand}" Height="34" Width="120" HorizontalAlignment="Right" />
        </Grid>

        <Image Source="{Binding HistoryItem.MediaPath}" />
    </DockPanel>
</CommonControls:DialogControl>