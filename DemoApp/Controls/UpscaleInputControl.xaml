<CommonControls:BaseControl x:Class="DemoApp.Controls.UpscaleInputControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
             xmlns:scm="clr-namespace:System.ComponentModel;assembly=WindowsBase"
             xmlns:base="clr-namespace:DemoApp"
             xmlns:local="clr-namespace:DemoApp.Controls"
             xmlns:Behaviors="clr-namespace:TensorStack.WPF.Behaviors;assembly=TensorStack.WPF"
             xmlns:CommonControls="clr-namespace:TensorStack.WPF.Controls;assembly=TensorStack.WPF"
             xmlns:CommonConverters="clr-namespace:TensorStack.WPF.Converters;assembly=TensorStack.WPF"
             xmlns:Controls="clr-namespace:DemoApp.Controls"
             xmlns:Common="clr-namespace:DemoApp.Common"
             xmlns:Views="clr-namespace:DemoApp.Views">
    <Grid  DataContext="{Binding RelativeSource={RelativeSource AncestorType={x:Type local:UpscaleInputControl}}}">
        <UniformGrid Columns="3">
            <StackPanel>
                <TextBlock Text="Tile Mode" Style="{StaticResource FieldTextBlockStyle}" />
                <ComboBox SelectedItem="{Binding Options.TileMode}" ItemsSource="{Binding Source={StaticResource TileMode}}" />
            </StackPanel>
            <StackPanel>
                <TextBlock Text="Tile Size" Style="{StaticResource FieldTextBlockStyle}" />
                <TextBox Text="{Binding Options.TileSize}" />
            </StackPanel>
            <StackPanel>
                <TextBlock Text="Tile Overlap" Style="{StaticResource FieldTextBlockStyle}" />
                <TextBox Text="{Binding Options.TileOverlap}" />
            </StackPanel>
        </UniformGrid>
    </Grid>
</CommonControls:BaseControl>
